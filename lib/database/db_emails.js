/*
emails = new Mongo.Collection("emails");


if (Meteor.isServer) {
  Meteor.publish('emails', function() {
    return emails.find()
  })
}


if (Meteor.isClient) {
  Meteor.subscribe("emails")
}


gmails = new Mongo.Collection("gmails");


if (Meteor.isServer) {
  Meteor.publish('gmails', function() {
    return gmails.find()
  })
}


if (Meteor.isClient) {
  Meteor.subscribe("gmails")
}

*/